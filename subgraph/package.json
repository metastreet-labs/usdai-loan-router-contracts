{
  "name": "metastreet-usdai-subgraph",
  "scripts": {
    "format": "prettier --write .",
    "codegen": "graph codegen subgraph.yaml",
    "build:arbitrum": "graph build --network arbitrum-one subgraph.yaml",
    "build:sepolia": "graph build --network sepolia subgraph.yaml",
    "deploy:arbitrum": "npm run build:arbitrum && sh -c 'VERSION=\"${1}\"; goldsky subgraph deploy loan_router_arbitrum/$VERSION' --",
    "deploy:sepolia": "npm run build:sepolia && sh -c 'VERSION=\"${1}\"; goldsky subgraph deploy loan_router_sepolia/$VERSION' --"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.97.1",
    "@graphprotocol/graph-ts": "0.37.0",
    "prettier": "3.6.2"
  }
}
